# SSE Tests - Not Yet Implemented

## Current Status
The SSE implementation is not yet complete, and therefore the current tests are failing. These tests serve as a template for future validation once the SSE implementation is complete.

## When to Use These Tests
These tests should be revisited when:
1. The controller endpoints for the MCP API are implemented
2. The mock controller (`MockMcpController.cs`) is potentially removed or modified to support SSE

## Other Tests
The Core and STDIO tests are functioning correctly with good coverage. 